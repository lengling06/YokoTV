# 使用淘宝镜像加速（如果在中国）
# registry=https://registry.npmmirror.com

# 提高并发数
fetch-retries=5
fetch-retry-mintimeout=20000
fetch-retry-maxtimeout=120000

# 禁用进度条（CI环境下）
progress=false

# 使用精确版本
save-exact=true

# 不生成 package-lock.json 的冗余信息
package-lock=true

# 减少日志输出
loglevel=warn

# 忽略脚本（构建时可选）
# ignore-scripts=true

# 使用缓存
prefer-offline=true

# 提高网络超时时间
network-timeout=600000

# 禁用审计（构建时）
audit=false

# 禁用资金提示
fund=false
